# 19 — Электрика: розетки, выключатели, провода, щиток 🔜

Первая реализация слоя `Electrical` из [07-mep-layers](07-mep-layers.md). UX выработан
панелью из трёх экспертов (VR-моторика · инженер-электрик · MR/HCI), 2026-08-10.
Принцип прежний: **всё — параметрические данные**, меши генерируются процедурно.

## Модель данных (Core)

```csharp
enum FixtureKind { Outlet, Switch, Panel }
enum CableType   { C3x15, C3x25 }            // "3x1.5" (свет), "3x2.5" (розетки)

// ElectricFixture : MonoBehaviour (Core, как Wall/Floor — тестируемо)
//   Kind, Posts (1..5, Outlet), Keys (1..3, Switch); позиция/ориентация — transform
//   (стоит на грани стены, yaw по нормали). Build() — процедурный меш, winding наружу.
//   TerminalWorld — точка ввода кабеля (центр верха блока; у щитка — низ).

// WireRoute : MonoBehaviour (Core)
//   Points (List<Vector3>, мировые, лежат на поверхностях), CableType,
//   StartFixtureId / EndFixtureId (логическая связь, string Id из SceneModel),
//   Build() — трубка Ø15 мм вдоль ломаной (8-гранник, winding наружу), Length.

// WireMath (static): OrthoElbow(prev, next) — одно колено, горизонталь на высоте
//   верхней из двух точек (v2: без авто-захода на потолок; вырожденные колена < 1 см
//   схлопываются); PolylineLength; BuildTube(points, radius, sides, mesh-листы).

// ElectricalBom (static): смета по списку трасс:
//   Σ длины по CableType + 0.15 м × кол-во подключений (привязанные концы)
//   + общий запас % → строки "3x1.5 — 24.6 m", "Total — 62.7 m".
```

Щиток — тоже `ElectricFixture` (Kind=Panel, 300×400×80 мм), **один на сцену в v1**.
Смету он собирает не по проводке ссылок, а **через реестр `SceneModel`** (все живые
нескрытые `WireRoute`) — как `WallController` находит плиты пола (паттерн C5).

## UX (сведённые решения панели)

- **Один инструмент «Elec»** (7-я кнопка палитры), суб-режимы — первый ряд инспектора:
  `Mode: Outlet → Switch → Wire → Panel` (Cycle). Акцент слоя `UiColors.LayerElectrical`
  #FFC94D — ретикл/кромки, но **не сами объекты**: провода графит #1A1A1A (чистый чёрный
  на passthrough «дыра»), розетки/выключатели/щиток — белые.
- **Фикстуры**: луч + триггер по грани стены (промах/пол = отказ + хаптика; «воздушных»
  точек нет). Высота **жёстко снапится к пресету** (Outlet 30 см, Switch 90 см,
  Panel 150 см до центра — от `Level`), грип во время постановки = свободная вертикаль.
  Превью — ghost-блок на стене. Подстройка высоты — степпером в инспекторе, не жестом
  (тремор луча ±3–5 см > шага степпера).
- **Провод**: точка за точкой триггером по стенам и потолку. Режим **Ortho** (дефолт):
  между кликами вставляются колени — вертикаль → горизонталь на «высоте прокладки»
  (**отступ от потолка**, ряд `Ceiling off`, дефолт 20 см; потолок = `Level + WallHeight`
  либо фактический Y точки) → вертикаль. Free — прямые сегменты. Углы комнаты пользователь
  проходит кликом на угол (v1; авто-излом по общему ребру — v2).
  **Завершение — B** (как FinishChain у Walls); **клик по щитку завершает трассу сам**.
  Конец/начало ближе 10 см к терминалу фикстуры — снап (квадратный ретикл) + запись
  логической связи: фикстура двигается — привязанный конец едет за ней.
  Дефолт кабеля от стартовой фикстуры: от Switch — 3x1.5, от Outlet — 3x2.5.
- **Щиток и смета**: метраж — в `Describe()` выбранного щитка (группа выделения
  инспектора): строки по типам кабеля + Total с запасами. Ряд `Reserve %` (0–30, шаг 5)
  — per-instance. Формула: Σ длин + 0.15 м × подключение + запас %.
- **Настройки инструмента** (дефолты для следующих объектов):
  Outlet: `Posts 1..5`, `Height см`; Switch: `Keys 1..3`, `Height см`;
  Wire: `Cable 3x1.5↔3x2.5`, `Route Ortho↔Free`, `Ceiling off см`; Panel: `Reserve %`.
  Per-instance те же ряды через `ISettingsProvider` выбранного объекта.
- **Редактирование v1**: через Select — фикстуру тащить (стандартный драг), B — удалить
  (DeleteCommand); у трассы `EditHandle`-ручки на точках (`RouteHandles : IHandleProvider`,
  один жест = одна команда). Смена суб-режима/инструмента с начатой трассой ≥2 точек —
  **авто-финиш**, не потеря.
- **Защита от грязного ввода**: дебаунс точки 250 мс + мин. шаг 3 см; пересечение
  габаритов фикстур (порог 5 см) — отказ; B по пустоте при рисовании = финиш трассы,
  Esc-семантика — со второго B.

## Скелет классов и размещение

| Файл | Что |
|---|---|
| `Core/ElectricalTypes.cs` | enums, пресеты высот/размеров, `CableSpec` (лейблы) |
| `Core/WireMath.cs` | орто-колени, длина ломаной, генератор трубки |
| `Core/WireRoute.cs` | вью трассы (меш+коллайдер, `OnDestroy` освобождает меш) |
| `Core/ElectricFixture.cs` | вью розетки/выключателя/щитка (один класс, Kind) |
| `Core/ElectricalBom.cs` | смета (чистая функция, юнит-тесты) |
| `Electrical/ElectricController.cs` | ITool «electric»/«Elec», суб-режимы, постановка |
| `Electrical/FixtureParameters.cs` | per-instance схема фикстуры (+Reserve у Panel) |
| `Electrical/RouteParameters.cs` | per-instance схема трассы (Cable) |
| `Electrical/RouteHandles.cs` | IHandleProvider точек трассы |
| `Editor/Setup/SetupElectricTool.cs` | префабы Fixture/Route + wiring |
| Общие точки (минимально) | `SelectableKind`+`Selectable.Resolve/Describe/MoveBy`, `RigContext`, `MeasureSetup`, `SetupPalette` (7 кнопок, ширина), `ToolManager` (поле+реестр), `SetupAssets` (WireMat/FixtureMat), `10-controls.md` |

Классификация поверхности под лучом — **по нормали хита** (`|n.y|<0.3` стена,
`n.y<-0.7` потолок, иначе отказ): работает и по EffectMesh скана, и по своим стенам.

## Осознанно отложено (v2)

- Светильники, распаячные коробки, слаботочка; несколько щитков; автоматы/номиналы.
- Авто-излом орто-сегмента по общему ребру смежных стен; прокладка по полу.
- Re-host фикстуры на другую стену; вставка точки в середину трассы; ветвление.
- Снап к проёмам (выключатель у наличника) — после интеграции openings.
- Layer-чипы палитры (eye/solo) — вместе со вторым MEP-слоем; экспорт сметы (08-interop).
- Перемещение щитка (в v1 — удалить и поставить заново).

## v2 — ручная трассировка и распределительные коробки (2026-08-10, вечер)

Фидбек шлема: «не нравится, что провод сам уходит на потолок; хочу вести руками —
довёл до стыка стены и потолка, поставил точку, дальше сам по потолку; две розетки
соединить напрямую нельзя; на потолке нужны распределительные коробки».

- **Авто-потолок удалён.** `OrthoWaypoints(prev, next, runY)` (вертикаль → магистраль
  на «высоте прокладки» → вертикаль) заменён на `OrthoElbow(prev, next)`: **одно**
  колено, горизонтальный пролёт идёт на высоте ВЕРХНЕЙ из двух точек (спуски
  вертикальны, магистраль по верху — как штробят). Провод оказывается на потолке
  только если пользователь сам кликнул по потолку. Розетка↔розетка на одной высоте —
  прямой горизонтальный сегмент, без «крюка».
- Ряд **«Ceiling off» удалён** из инспектора; `CeilingY()/RunY()` удалены из
  контроллера. Константы `CeilingOffset/Min/Max/Step` в `ElectricalTypes` остались как
  legacy v1 (их может использовать личная ветка) — в main не читаются.
- **Распределительная коробка** — `FixtureKind.Junction`: куб 80×80 мм, глубина 45 мм,
  с крышкой; белая, как остальные фикстуры. Ставится на **стену ИЛИ потолок** (для
  потолка ориентация по нормали вниз), высота свободная — пресета и грипа нет.
  Терминал — центр крышки. Снап конца трассы 10 см и «клик по терминалу = финиш»
  работают как у прочих фикстур; **ветвление = несколько трасс, привязанных к одной
  коробке** (id-связи уже это умеют). Смета: каждый привязанный конец уже даёт
  +0.15 м подключения; счёт коробок в BOM — v3.
- Суб-режимы стали: `Outlet / Switch / Wire / Box / Panel` (таб Box — иконка
  `junction-box`). `Describe()` коробки: `Junction box · N wires`.

## Интеграция с main (2026-08-10, после мерджа v1)

Фидбек первой примерки в шлеме — три стыка с системами main, починены на стороне main:

- **Телепорт**: `TeleportCommand` теперь сдвигает и электрику (фикстуры `MoveBy` +
  `BaseLevel += delta.y`, чтобы высоты «над этажом» пережили вертикальный телепорт;
  трассы `MoveBy` целиком — привязанные концы не двигаются дважды). Без этого розетки
  и провода визуально «ехали вместе с пользователем».
- **Сброс проекта**: `ImportController.ClearScene` («New project») уничтожает
  ЗАРЕГИСТРИРОВАННЫЕ в `SceneModel` фикстуры/трассы; припаркованный шаблон префаба и
  ghost-превью инструмента не зарегистрированы и переживают сброс.
- **Реальный потолок**: `CeilingY()` больше не берёт `manager.Level + WallHeight`
  (высота ИНСТРУМЕНТА стен, 2.7 м — в импортированном доме потолки 3 м+). Теперь —
  луч вверх с высоты колена в XZ точки: ближайшая грань «смотрящая вниз» не ниже 2 м
  над полом (низ мебели отсекается порогом, свои трассы/ручки — фильтром по
  `WireRoute`); фолбэк — прежняя формула. Отступ трассы от потолка остаётся
  настройкой «Ceiling off» (10–50 см, по умолчанию 20).

## Продолжение трассы от свободного конца (#81, фидбек 2026-08-15)

Провод, законченный «в воздухе» (например, на потолке без привязки), можно
**продолжить**: в суб-режиме Wire курсор магнитится к свободным концам существующих
трасс (радиус — как у терминалов, 10 см; конец с привязкой к фикстуре свободным не
считается, скрытые трассы не магнитят). Клик подхватывает трассу: её полилиния
становится активной, рисование продолжается тем же проводом; подхват за НАЧАЛО
разворачивает полилинию (привязки Start/End меняются местами), чтобы точки всегда
дописывались в хвост. Финиш (терминал / B / смена режима) переписывает **ту же**
`WireRoute` одной командой `RouteExtendCommand` (before/after: точки, привязки,
кабель) — одна запись undo на всё продолжение; финиш без новых точек — no-op.
Перетаскивание точек (включая концы) было и остаётся в Select через `RouteHandles`.

## Внешний вид фурнитуры v2 (issue #134, фидбек шлема 2026-08-16)

«Розетки, выключатели и щиток — с текстурами и пореалистичнее». Было: коробки с
приклеенными бобышками. Стало (`Core/ElectricFixture`, параметрика, без ассетов):

- **Фаска 1.2 мм** на всех видимых пластиковых деталях — ребро ловит свет, а не
  заканчивается бритвой.
- **Розетка** — настоящее *утопленное* гнездо Ø53 мм на каждый пост: лицевая грань
  плиты строится **кольцом вокруг отверстия** (`AddFaceWithRoundHoles`), внутри —
  стенки и дно, два штырьковых отверстия и боковые контакты заземления.
- **Выключатель** — клавиши с реальным зазором 3.5 мм и наклоном (низ утоплен, верх
  выступает), позади — тёмный колодец: тень по контуру и делает силуэт узнаваемым.
- **Щиток** — открытый спереди металлический корпус, два ряда автоматов и DIN-реек,
  ручка и петли. Дверца переключается между закрытым состоянием с настоящим
  инспекционным окном (`AddFaceWithRectHole`) и открытым положением с поворотом на петле.
- **Распаячная коробка** — крышка с фаской и четырьмя утопленными винтами.
- **Материалы**: сабмеш 0 — пластик (на него ложится краска и отделка), сабмеш 1 —
  тёмная фурнитура (штыри, винты, рейки, ручка), сабмеш 2 — корпус и дверца щитка из
  `metal-brushed` с normal map. Пластик берёт `plastic-white` и процедурный микрорельеф;
  обе поверхности используют **метрические UV** фурнитуры.
- **Состояние экземпляра**: белый/чёрный вариант доступен как `Swatch`, щиток имеет
  переключатель `Open`. Оба значения проходят через undo/redo и сохраняются в проекте;
  смена цвета выполняется property block без перестройки меша.

Грабли, пойманные тестом объёма (правило 1.1): выемка обязана **вычитать** объём —
стенки цилиндра смотрят к оси, кольцо лицевой грани — в комнату. Оба раза первая
версия была вывернута, и `Meshes_AreWoundOutward_PositiveVolume` это поймал. Визуальный
контроль — headless-скриншот `Build/ui-shots/electric-fixtures.png` (материал
двусторонний, так что на глаз вывернутая грань не видна — только по объёму).
